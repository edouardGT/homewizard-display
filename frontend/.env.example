# Base URL of the HomeWizard backend API.
# Local dev: http://localhost:5050
# Production / Docker (served same-origin via nginx): leave empty to use relative /api
VITE_API_BASE_URL=http://localhost:5050
